# Use latest Node.js LTS Alpine image
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Copy dependency files first
COPY package*.json ./

# Install only production dependencies
RUN npm ci --only=production

# Copy application source code
COPY . .

# Expose app port
EXPOSE 3000

# Run the app
CMD ["node", "server.js"]
